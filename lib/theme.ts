/**
 * Shared dark frosted-glass tokens for every section below the hero (ticket 4.1). Mirrors the
 * light glassmorphism system in `reviewguide-app/lib/theme.ts` (rounded-2xl, translucent
 * surface, backdrop-blur, subtle border+shadow) inverted for a dark background.
 */
export const DARK_GLASS_CARD =
  "rounded-2xl border border-white/10 bg-white/[0.04] backdrop-blur-xl shadow-lg shadow-black/40";

export const SECTION = "mx-auto max-w-5xl px-6 py-20 sm:py-28";

export const SECTION_EYEBROW = "text-sm font-semibold tracking-wide text-amber-200/80 uppercase";

export const SECTION_HEADING = "mt-3 text-3xl font-semibold tracking-tight text-white sm:text-4xl";
